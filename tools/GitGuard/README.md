# GitGuard

Local-only Git guard for Auto Theme Solar Engine maintenance.

Use it to check Git identity, allowed remotes, branch policy, commit messages,
changelog synchronization, and repository path safety before pushing.

The implementation files in this directory are intentionally ignored by Git.
Only this README is tracked.
