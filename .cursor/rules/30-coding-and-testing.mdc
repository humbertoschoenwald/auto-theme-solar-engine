---
description: Coding and testing rules derived from repository implementation standards.
globs:
  - "src/**/*.cs"
  - "tests/**/*.cs"
  - "Directory.Build.props"
  - "Directory.Build.targets"
  - ".editorconfig"
alwaysApply: false
---

@file ../../docs/adr/0009-coding-and-testing-standards.md
@file ../../docs/adr/0014-source-only-static-policy-enforcement.md
@file ../../docs/adr/0016-modular-monolith-and-slice-boundaries.md
@file ../../docs/adr/0017-explicit-failure-flow.md
@file ../../docs/adr/0018-relational-data-access-applicability.md

# Coding And Testing

- RULE: Keep the repository as a modular monolith with feature slices instead of generic layer folders.
- RULE: Keep code organized by feature and domain boundaries.
- RULE: Use `Result` or `Result<T>` for expected failures. Reserve exceptions for invalid states and boundary failures.
- RULE: Prefer early returns over deeply nested ternaries when validating or normalizing inputs.
- RULE: Do not use `System.InvalidOperationException`. Use `Result<T>`, a more specific framework exception, or a dedicated repository exception that names the broken invariant.
- RULE: Do not author new `DllImport` declarations. Use `LibraryImport`.
- RULE: Do not keep owned native resources in naked `nint` fields. Use typed
  `SafeHandle` wrappers for owned icons, menus, fonts, brushes, and similar
  resources.
- RULE: Do not add EF Core, Dapper, or relational database plumbing unless a concrete repository feature introduces relational persistence and ADR is updated accordingly.
- RULE: Repository-only forbidden-type checks must scan authored source under `src/` and `tests/`, not generated files under `obj/` or `bin/`.
- RULE: Keep comments sparse. Repository rationale belongs in ADR. Inline comments are for local hazards only.
- RULE: Public XML docs describe behavior when public APIs exist. They do not restate repository doctrine.
- RULE: Use Allman braces, four-space indentation, explicit visibility-first
  modifiers, top-of-file using directives outside namespaces, and repository
  field naming (`_`, `s_`, `t_`) in authored C#.
- RULE: Prefer `readonly`, language keywords, `nameof(...)`, PascalCase method
  names, and at most one blank line in a row.
- RULE: Repository C# style is a strict superset. Existing stricter rules still
  win where a broader guideline would allow omitted braces, `var`, or
  target-typed `new()`.
- RULE: Keep internal and private types `static` or `sealed` unless derivation
  is required, and keep primary constructor parameters parameter-shaped.
- RULE: Tests must be deterministic, granular, and include regressions for bug fixes.
- RULE: Do not add tautological tests or cardboard stubs. Tests must exercise
  observable behavior with meaningful collaborators.
- RULE: Use `TimeProvider` when time affects behavior.
