#!/usr/bin/env bash
set -euo pipefail

if ! command -v npx >/dev/null 2>&1; then
  echo "npx is required to validate commit messages. Install Node.js first."
  exit 1
fi

npx --no -- commitlint --edit "$1"
